import { StyleSheet } from 'react-native';
import { PRIMARY_COLOR, SECONDARY_COLOR } from 'components/theme';

export const styles = StyleSheet.create({
	main: {
		backgroundColor: PRIMARY_COLOR
	},
	container: {
		paddingLeft: '10%',
		paddingRight: '10%'
	}
});
